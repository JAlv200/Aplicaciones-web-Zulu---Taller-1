@if(state is null)
{
    <Loading Label="Cargando estado, por favor espere ..."/>
}
else
{
    <MudDialog>
        <DialogContent>
            <FormWithName Label="Estado:" Model="state" OnValidSubmit="EditAsync" ReturnAction="Return"/>
        </DialogContent>
    </MudDialog>
}
